using System;


namespace Estudos_Memoria_1
{
    struct Pontos
    {
        public double X, Y;

        public override string ToString()
        {
            return "(" + X + " || " + Y + ")";
        }
    }
}